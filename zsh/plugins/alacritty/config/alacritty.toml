[general]
import = ["~/.config/alacritty/themes/catppuccin-frappe.toml"]

[keyboard]
bindings = [{ key = "Return", mods = "Shift", chars = "\n" }]

[window]
padding = { x = 8, y = 8 }

[env]
TERM = "xterm-256color"

[terminal]
[terminal.shell]
program = "zsh"
args = ["-l", "-c", "tmux"]
